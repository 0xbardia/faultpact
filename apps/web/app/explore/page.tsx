import Link from "next/link";
import { Shell } from "../../components/shell";
import { LandingStats } from "../../components/explorer";
import { PageHeader, Panel, ProductGlyph } from "../../components/ui";

export default function ExplorePage() {
  return <Shell><main className="content-shell explore-home"><PageHeader eyebrow="Public explorer" title="Read the exchange before you connect." description="Indexed protocol state from the frozen GenLayer Studio Dev deployment. Financial truth remains onchain; this surface is for discovery and context." /><LandingStats /><div className="explore-grid">{[["providers", "Providers", "Bonded operators and their service commitments.", "bond"], ["services", "Services", "The infrastructure being protected.", "service"], ["pacts", "Pacts", "Immutable SLA terms and current capacity.", "pact"], ["coverages", "Coverages", "Protection positions and status.", "coverage"], ["incidents", "Incidents", "Evidence, canonical facts, and challenges.", "incident"], ["claims", "Claims", "Deterministic eligibility and settlement.", "settlement"]].map(([href, title, description, glyph]) => <Link className="explore-card" href={`/${href}`} key={href}><ProductGlyph kind={glyph as "pact"} /><span className="explore-card-index">/0{["providers", "services", "pacts", "coverages", "incidents", "claims"].indexOf(href) + 1}</span><h2>{title}</h2><p>{description}</p><span className="text-link">Browse {title.toLowerCase()} →</span></Link>)}</div><Panel className="explore-callout"><div><p className="eyebrow">Need the model first?</p><h2>Learn how evidence becomes a claim.</h2><p>Start with the protocol terms, then follow an Incident from observation to final settlement.</p></div><Link className="button button-coral" href="/docs">Read documentation ↗</Link></Panel></main></Shell>;
}
